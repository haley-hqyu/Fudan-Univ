测试函数在脚本test.m中；
第一段为解方程并绘图的程序；
解第二类fredholm方程，其中积分区间默认为[0,1]（由于taylor级数是有收敛域的，故需在有限区间上考虑，并选取taylor展开收敛性较好的函数）；
可以修改的参数有：积分核kernel（符号函数），右端项y（符号函数），lambda，taylor展开项数n（整数）；
第二段为测试函数；
测试问题为5*x(t)-\int_0^1 \exp(s*t) *x(t)=y(t),真解为x(t)=exp(t)；
最终结果有两张图片，第一张为n=3,5,7时的计算解和真解的函数图象，第二张为误差半对数图；